const sumOfNumber = (x:number): number=>{
    let num: string = ;
    console.log(num)
    // let newNum = num.reduce((prev, curr)=>prev + curr)
    return 0;
}

let ans = sumOfNumber(4521);
console.log(ans)
export{}